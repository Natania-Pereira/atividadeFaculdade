// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff9ebf', // Rosa claro
    },
    secondary: {
      main: '#ff9ebf', // Rosa claro
    },
  },
});

export default theme;
